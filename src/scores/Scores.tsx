import { Button, Text, View } from "react-native";

import { styles } from "./styles";

export const Scores = () => {
  return (
    <View style={styles.scores}>
      <Text>Scores</Text>
    </View>
  );
};
